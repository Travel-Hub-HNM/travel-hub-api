services:
  app:
    image: kangminhyeok/travel-hub-api:latest
    container_name: travel-hub-api
    ports:
      - "9700:9700"
    volumes:
      - ./logback-spring.xml:/app/logback-spring.xml
      - ./log:/app/log
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - LOGGING_CONFIG=/app/logback-spring.xml